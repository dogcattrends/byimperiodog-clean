# Example Playwright environment variables
# Copy to .env.playwright (gitignored) and fill with valid admin credentials before running tests
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme
PLAYWRIGHT_BASE_URL=http://localhost:3000
# Optional: set CI=true in CI environments
# CI=true
