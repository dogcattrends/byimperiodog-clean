/* eslint-disable no-console */
import 'dotenv/config'

console.log('[generate-embeddings] Script legado descontinuado.')
console.log('[generate-embeddings] Use: npm run embeddings:index')

process.exit(0)